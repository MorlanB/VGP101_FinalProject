#include "helper.h"
#include "questions.h"

void editorMain(struct Question* questions) {
	while (1) {
		int temp = getInt("this is a test editor, enter 0 to return to menu");
		if (temp == 0) break;
	}
}